package com.lowjungxuan.proz.newFolder.intention_action

enum class SnippetType {
    Obx, GetBuilder, GetBuilderAutoDispose, GetX, BlocBuilder, BlocListener, BlocProvider, BlocConsumer, RepositoryProvider,
}